server:
  port: 9191
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pharmacy_db
    username: root
    password: your password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: nishitasalvi9@gmail.com
    password: your password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
BASE_URL: http://localhost:9191/
STRIPE_PUBLIC_KEY: your public stripe key
STRIPE_SECRET_KEY: your private stripe key
